<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm sticky-top">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center gap-2 fw-bold" href="{{ path('user_dashboard') }}">
            <i class="bi bi-film text-primary fs-3"></i>
            <span>ClubCine</span>
        </a>

        {% if app.user %}
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarMain">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 mt-3 mt-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center gap-2" href="#" id="groupsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-people-fill"></i> Mis Clubes
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark shadow border-secondary" aria-labelledby="groupsDropdown">
                            <li><h6 class="dropdown-header">Tus Clubes activos</h6></li>
                            {% if app.user.group %}
                                <li>
                                    <a class="dropdown-item d-flex justify-content-between align-items-center" href="{{ path('app_group_show', {id: app.user.group.id}) }}">
                                        <span>
                                            <i class="bi bi-circle-fill text-success me-2" style="font-size: 0.5rem;"></i> 
                                            {{ app.user.group.name }}
                                        </span>
                                        <i class="bi bi-chevron-right small opacity-50"></i>
                                    </a>
                                </li>
                            {% else %}
                                <li><span class="dropdown-item-text small text-muted">Aún no tienes club</span></li>
                            {% endif %}

                            <li><hr class="dropdown-divider border-secondary"></li>
                            <li>
                                <a class="dropdown-item text-primary d-flex align-items-center gap-2" href="#" data-bs-toggle="modal" data-bs-target="#createGroupModal">
                                    <i class="bi bi-plus-circle-fill"></i> <strong>Fundar nuevo club</strong>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item d-flex align-items-center gap-2" href="#">
                                    <i class="bi bi-pencil-square"></i> Editar club
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item text-danger d-flex align-items-center gap-2" href="#">
                                    <i class="bi bi-box-arrow-left"></i> Salirse del club
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>

                <div class="d-flex align-items-center ms-auto gap-3 pt-3 pt-lg-0 mt-3 mt-lg-0 border-lg-0">
                    <span class="text-secondary small d-none d-md-inline">
                        <i class="bi bi-person-circle me-1"></i>
                        <span class="text-white">{{ app.user.name|default(app.user.email|split('@')[0]) }}</span>
                    </span>
                    <a href="{{ path('auth_logout') }}" class="btn btn-sm btn-outline-danger d-flex align-items-center gap-2 px-3 shadow-sm">
                        <i class="bi bi-power"></i>
                        <span>Salir</span>
                    </a>
                </div>
            </div>
        {% else %}
            <div class="ms-auto">
                <a href="{{ path('auth_login') }}" class="btn btn-sm btn-primary px-4">Entrar</a>
            </div>
        {% endif %}
    </div>
</nav>