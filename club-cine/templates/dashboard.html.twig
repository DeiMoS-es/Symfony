{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="container my-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="">Bienvenido {{ app.user.email }}</h1>
        <div class="text-center">
                <a href="{{ path('auth_logout') }}" class="btn btn-danger">Cerrar sesión</a>
        </div>
    </div>

  {# Incluimos el catálogo como partial #}
  {% include 'movie/catalog.html.twig' with { movies: movies } %}
</div>
{% endblock %}
