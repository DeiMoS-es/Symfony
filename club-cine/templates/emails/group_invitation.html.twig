{# templates/emails/group_invitation.html.twig #}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invitación a {{ groupName }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f9f9f9;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            padding: 0;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }
        .content {
            padding: 40px 30px;
        }
        .greeting {
            font-size: 16px;
            margin-bottom: 20px;
            color: #555;
        }
        .message {
            background-color: #f0f4ff;
            border-left: 4px solid #667eea;
            padding: 20px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .message p {
            margin: 0 0 10px 0;
            color: #444;
        }
        .group-name {
            font-size: 18px;
            font-weight: 600;
            color: #667eea;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 14px 40px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 600;
            margin: 30px 0;
            text-align: center;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }
        .cta-link {
            word-break: break-all;
            background-color: #f5f5f5;
            padding: 15px;
            border-radius: 4px;
            font-family: monospace;
            font-size: 12px;
            color: #667eea;
            margin: 20px 0;
            border: 1px solid #e0e0e0;
        }
        .footer {
            background-color: #f9f9f9;
            padding: 20px;
            text-align: center;
            font-size: 12px;
            color: #999;
            border-top: 1px solid #eee;
        }
        .expiry-warning {
            background-color: #fff3cd;
            border: 1px solid #ffc107;
            border-radius: 4px;
            padding: 15px;
            margin: 20px 0;
            font-size: 13px;
            color: #856404;
        }
        .expiry-warning strong {
            color: #cc0000;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🎬 Invitación a {{ groupName }}</h1>
        </div>

        <!-- Content -->
        <div class="content">
            <p class="greeting">¡Hola!</p>

            <div class="message">
                <p>Has sido invitado a unirte al grupo <span class="group-name">{{ groupName }}</span> en Club de Cine.</p>
                <p>Haz clic en el botón de abajo para aceptar la invitación:</p>
            </div>

            <!-- CTA Button -->
            <div style="text-align: center;">
                <a href="{{ acceptUrl }}" class="cta-button">Aceptar Invitación</a>
            </div>

            <!-- Alternative Link -->
            <p style="text-align: center; color: #999; font-size: 13px;">
                O copia y pega este enlace en tu navegador:
            </p>
            <div class="cta-link">
                {{ acceptUrl }}
            </div>

            <!-- Expiry Warning -->
            <div class="expiry-warning">
                <strong>⚠️ Importante:</strong> Esta invitación expira el 
                <strong>{{ expiresAt|date('d/m/Y H:i') }}</strong>. 
                Asegúrate de aceptarla antes de que expire.
            </div>

            <!-- Footer Text -->
            <p style="color: #888; font-size: 14px; margin-top: 30px;">
                Si no solicitaste esta invitación, puedes ignorar este email. 
                No se realizará ninguna acción a menos que hagas clic en el enlace.
            </p>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>
                © {{ 'now'|date('Y') }} Club de Cine. Todos los derechos reservados.<br>
                <a href="https://clubdecine.com" style="color: #667eea; text-decoration: none;">Visita nuestro sitio</a>
            </p>
        </div>
    </div>
</body>
</html>